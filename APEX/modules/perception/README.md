# Perception Module

This module currently contains the Arduino IMU serial ingestion path:

- Arduino Nano 33 IoT IMU stream (`arduino/`)
- ROS2 serial bridge (`nano_accel_serial_node`)

Future expansion:
- LiDAR and camera fusion
- sensor timestamp synchronization
