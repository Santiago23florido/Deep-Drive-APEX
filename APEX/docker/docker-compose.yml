services:
  apex_pipeline:
    container_name: apex_pipeline
    build:
      context: ..
      dockerfile: docker/Dockerfile
      network: host
    restart: "no"
    privileged: true
    network_mode: host
    devices:
      - "${APEX_SERIAL_PORT:-/dev/ttyACM0}:${APEX_SERIAL_PORT:-/dev/ttyACM0}"
      - "${APEX_LIDAR_PORT:-/dev/ttyUSB0}:${APEX_LIDAR_PORT:-/dev/ttyUSB0}"
    environment:
      ROS_DOMAIN_ID: "${ROS_DOMAIN_ID:-30}"
      ROS_AUTOMATIC_DISCOVERY_RANGE: "${ROS_AUTOMATIC_DISCOVERY_RANGE:-SUBNET}"
      RMW_IMPLEMENTATION: "${RMW_IMPLEMENTATION:-rmw_fastrtps_cpp}"
      APEX_GIT_COMMIT: "${APEX_GIT_COMMIT:-unknown}"
      APEX_GIT_DIRTY: "${APEX_GIT_DIRTY:-0}"
      APEX_SERIAL_PORT: "${APEX_SERIAL_PORT:-/dev/ttyACM0}"
      APEX_BAUDRATE: "${APEX_BAUDRATE:-115200}"
      APEX_I2C_BUS: "${APEX_I2C_BUS:-/dev/i2c-1}"
      APEX_LIDAR_PORT: "${APEX_LIDAR_PORT:-/dev/ttyUSB0}"
      APEX_LIDAR_BAUDRATE: "${APEX_LIDAR_BAUDRATE:-115200}"
      APEX_LIDAR_HEADING_OFFSET_DEG: "${APEX_LIDAR_HEADING_OFFSET_DEG:-}"
      APEX_LIDAR_X_M: "${APEX_LIDAR_X_M:-}"
      APEX_LIDAR_Y_M: "${APEX_LIDAR_Y_M:-}"
      APEX_LIDAR_Z_M: "${APEX_LIDAR_Z_M:-}"
      APEX_LIDAR_ROLL_RAD: "${APEX_LIDAR_ROLL_RAD:-}"
      APEX_LIDAR_PITCH_RAD: "${APEX_LIDAR_PITCH_RAD:-}"
      APEX_LIDAR_YAW_RAD: "${APEX_LIDAR_YAW_RAD:-}"
      APEX_BASE_FRAME: "${APEX_BASE_FRAME:-}"
      APEX_LIDAR_FRAME: "${APEX_LIDAR_FRAME:-}"
      APEX_ENABLE_RECON_MAPPING: "${APEX_ENABLE_RECON_MAPPING:-0}"
      APEX_ENABLE_KINEMATICS: "${APEX_ENABLE_KINEMATICS:-1}"
      APEX_ENABLE_IMU_LIDAR_FUSION: "${APEX_ENABLE_IMU_LIDAR_FUSION:-0}"
      APEX_ENABLE_CURVE_ENTRY_PLANNER: "${APEX_ENABLE_CURVE_ENTRY_PLANNER:-0}"
      APEX_ENABLE_PATH_TRACKER: "${APEX_ENABLE_PATH_TRACKER:-0}"
      APEX_ENABLE_RECOGNITION_TOUR_PLANNER: "${APEX_ENABLE_RECOGNITION_TOUR_PLANNER:-0}"
      APEX_ENABLE_RECOGNITION_TOUR_TRACKER: "${APEX_ENABLE_RECOGNITION_TOUR_TRACKER:-0}"
      APEX_RECOGNITION_TRACKER_PUBLISH_UNARMED_ZERO_CMD: "${APEX_RECOGNITION_TRACKER_PUBLISH_UNARMED_ZERO_CMD:-}"
      APEX_RECOGNITION_TRACKER_PATH_TOPIC: "${APEX_RECOGNITION_TRACKER_PATH_TOPIC:-}"
      APEX_RECOGNITION_TRACKER_PLANNING_STATUS_TOPIC: "${APEX_RECOGNITION_TRACKER_PLANNING_STATUS_TOPIC:-}"
      APEX_RECOGNITION_TRACKER_ODOM_TOPIC: "${APEX_RECOGNITION_TRACKER_ODOM_TOPIC:-}"
      APEX_RECOGNITION_TRACKER_FUSION_STATUS_TOPIC: "${APEX_RECOGNITION_TRACKER_FUSION_STATUS_TOPIC:-}"
      APEX_RECOGNITION_TRACKER_STATUS_TOPIC: "${APEX_RECOGNITION_TRACKER_STATUS_TOPIC:-}"
      APEX_RECOGNITION_TRACKER_REQUIRE_ARM: "${APEX_RECOGNITION_TRACKER_REQUIRE_ARM:-}"
      APEX_RECOGNITION_TRACKER_DEFAULT_ARMED: "${APEX_RECOGNITION_TRACKER_DEFAULT_ARMED:-}"
      APEX_RECOGNITION_TRACKER_PATH_STALE_MAX_AGE_S: "${APEX_RECOGNITION_TRACKER_PATH_STALE_MAX_AGE_S:-}"
      APEX_RECOGNITION_TRACKER_PATH_STALE_ABORT_HOLD_S: "${APEX_RECOGNITION_TRACKER_PATH_STALE_ABORT_HOLD_S:-}"
      APEX_RECOGNITION_TRACKER_REAR_AXLE_OFFSET_X_M: "${APEX_RECOGNITION_TRACKER_REAR_AXLE_OFFSET_X_M:-}"
      APEX_RECOGNITION_TRACKER_REAR_AXLE_OFFSET_Y_M: "${APEX_RECOGNITION_TRACKER_REAR_AXLE_OFFSET_Y_M:-}"
      APEX_RECOGNITION_TRACKER_ODOM_TIMEOUT_S: "${APEX_RECOGNITION_TRACKER_ODOM_TIMEOUT_S:-}"
      APEX_ENABLE_CMDVEL_ACTUATION_BRIDGE: "${APEX_ENABLE_CMDVEL_ACTUATION_BRIDGE:-0}"
      APEX_ENABLE_MANUAL_CONTROL_BRIDGE: "${APEX_ENABLE_MANUAL_CONTROL_BRIDGE:-0}"
      APEX_ENABLE_RECOGNITION_SESSION_MANAGER: "${APEX_ENABLE_RECOGNITION_SESSION_MANAGER:-0}"
      APEX_ENABLE_OFFLINE_SUBMAP_REFINER: "${APEX_ENABLE_OFFLINE_SUBMAP_REFINER:-0}"
      APEX_ENABLE_FIXED_MAP_ROUTE_PLANNER: "${APEX_ENABLE_FIXED_MAP_ROUTE_PLANNER:-0}"
      APEX_ENABLE_FIXED_MAP_PUBLISHER: "${APEX_ENABLE_FIXED_MAP_PUBLISHER:-0}"
      APEX_ESTIMATION_BACKEND: "${APEX_ESTIMATION_BACKEND:-}"
      APEX_FUSION_ODOM_TOPIC: "${APEX_FUSION_ODOM_TOPIC:-}"
      APEX_FUSION_PATH_TOPIC: "${APEX_FUSION_PATH_TOPIC:-}"
      APEX_FUSION_POSE_TOPIC: "${APEX_FUSION_POSE_TOPIC:-}"
      APEX_FUSION_LIVE_MAP_TOPIC: "${APEX_FUSION_LIVE_MAP_TOPIC:-}"
      APEX_FUSION_FULL_MAP_TOPIC: "${APEX_FUSION_FULL_MAP_TOPIC:-}"
      APEX_FUSION_STATUS_TOPIC: "${APEX_FUSION_STATUS_TOPIC:-}"
      APEX_FUSION_ODOM_FRAME_ID: "${APEX_FUSION_ODOM_FRAME_ID:-}"
      APEX_FIXED_MAP_RUN_DIR: "${APEX_FIXED_MAP_RUN_DIR:-}"
      APEX_FIXED_MAP_DIR: "${APEX_FIXED_MAP_DIR:-}"
      APEX_FIXED_MAP_YAML: "${APEX_FIXED_MAP_YAML:-}"
      APEX_FIXED_MAP_DISTANCE_NPY: "${APEX_FIXED_MAP_DISTANCE_NPY:-}"
      APEX_FIXED_MAP_VISUAL_POINTS_CSV: "${APEX_FIXED_MAP_VISUAL_POINTS_CSV:-}"
      APEX_FIXED_MAP_ROUTE_CSV: "${APEX_FIXED_MAP_ROUTE_CSV:-}"
      APEX_FIXED_MAP_BUILD_STATUS_JSON: "${APEX_FIXED_MAP_BUILD_STATUS_JSON:-}"
      APEX_FIXED_MAP_AUTOBUILD: "${APEX_FIXED_MAP_AUTOBUILD:-}"
      APEX_FIXED_MAP_BUILDER_SCRIPT: "${APEX_FIXED_MAP_BUILDER_SCRIPT:-}"
      APEX_FIXED_MAP_PUBLISHER_SCRIPT: "${APEX_FIXED_MAP_PUBLISHER_SCRIPT:-}"
      APEX_FIXED_MAP_RESOLUTION_M: "${APEX_FIXED_MAP_RESOLUTION_M:-}"
      APEX_FIXED_MAP_MARGIN_M: "${APEX_FIXED_MAP_MARGIN_M:-}"
      APEX_FIXED_MAP_CLEARANCE_M: "${APEX_FIXED_MAP_CLEARANCE_M:-}"
      APEX_FIXED_MAP_CLEARANCE_SOFT_M: "${APEX_FIXED_MAP_CLEARANCE_SOFT_M:-}"
      APEX_FIXED_MAP_CORRIDOR_HALF_WIDTH_M: "${APEX_FIXED_MAP_CORRIDOR_HALF_WIDTH_M:-}"
      APEX_FIXED_MAP_CORRIDOR_RETRY_HALF_WIDTHS_M: "${APEX_FIXED_MAP_CORRIDOR_RETRY_HALF_WIDTHS_M:-}"
      APEX_FIXED_MAP_ROUTE_STEP_M: "${APEX_FIXED_MAP_ROUTE_STEP_M:-}"
      APEX_FIXED_MAP_FRAME_ID: "${APEX_FIXED_MAP_FRAME_ID:-}"
      APEX_FIXED_MAP_GRID_TOPIC: "${APEX_FIXED_MAP_GRID_TOPIC:-}"
      APEX_FIXED_MAP_VISUAL_POINTS_TOPIC: "${APEX_FIXED_MAP_VISUAL_POINTS_TOPIC:-}"
      APEX_FIXED_MAP_RECORDED_PATH_TOPIC: "${APEX_FIXED_MAP_RECORDED_PATH_TOPIC:-}"
      APEX_FIXED_MAP_STATUS_TOPIC: "${APEX_FIXED_MAP_STATUS_TOPIC:-}"
      APEX_FIXED_MAP_ROUTE_PATH_TOPIC: "${APEX_FIXED_MAP_ROUTE_PATH_TOPIC:-}"
      APEX_FIXED_MAP_ROUTE_STATUS_TOPIC: "${APEX_FIXED_MAP_ROUTE_STATUS_TOPIC:-}"
      APEX_OFFLINE_REFINER_SCRIPT: "${APEX_OFFLINE_REFINER_SCRIPT:-}"
      APEX_OFFLINE_REFINER_SCAN_TOPIC: "${APEX_OFFLINE_REFINER_SCAN_TOPIC:-}"
      APEX_OFFLINE_REFINER_IMU_TOPIC: "${APEX_OFFLINE_REFINER_IMU_TOPIC:-}"
      APEX_OFFLINE_REFINER_SEED_ODOM_TOPIC: "${APEX_OFFLINE_REFINER_SEED_ODOM_TOPIC:-}"
      APEX_OFFLINE_REFINER_SEED_STATUS_TOPIC: "${APEX_OFFLINE_REFINER_SEED_STATUS_TOPIC:-}"
      APEX_OFFLINE_REFINER_FRAME_ID: "${APEX_OFFLINE_REFINER_FRAME_ID:-}"
      APEX_OFFLINE_REFINER_CHILD_FRAME_ID: "${APEX_OFFLINE_REFINER_CHILD_FRAME_ID:-}"
      APEX_OFFLINE_REFINER_MAP_TOPIC: "${APEX_OFFLINE_REFINER_MAP_TOPIC:-}"
      APEX_OFFLINE_REFINER_GRID_TOPIC: "${APEX_OFFLINE_REFINER_GRID_TOPIC:-}"
      APEX_OFFLINE_REFINER_PATH_TOPIC: "${APEX_OFFLINE_REFINER_PATH_TOPIC:-}"
      APEX_OFFLINE_REFINER_SUBMAP_TOPIC: "${APEX_OFFLINE_REFINER_SUBMAP_TOPIC:-}"
      APEX_OFFLINE_REFINER_STATUS_TOPIC: "${APEX_OFFLINE_REFINER_STATUS_TOPIC:-}"
      APEX_OFFLINE_REFINER_ODOM_TOPIC: "${APEX_OFFLINE_REFINER_ODOM_TOPIC:-}"
      APEX_OFFLINE_REFINER_GLOBAL_CORRECTION_TOPIC: "${APEX_OFFLINE_REFINER_GLOBAL_CORRECTION_TOPIC:-}"
      APEX_OFFLINE_REFINER_ANCHOR_POSE_TOPIC: "${APEX_OFFLINE_REFINER_ANCHOR_POSE_TOPIC:-}"
      APEX_OFFLINE_REFINER_SEED_ODOM_FRAME_ID: "${APEX_OFFLINE_REFINER_SEED_ODOM_FRAME_ID:-}"
      APEX_OFFLINE_REFINER_WINDOW_SCAN_COUNT: "${APEX_OFFLINE_REFINER_WINDOW_SCAN_COUNT:-}"
      APEX_OFFLINE_REFINER_WINDOW_OVERLAP_COUNT: "${APEX_OFFLINE_REFINER_WINDOW_OVERLAP_COUNT:-}"
      APEX_OFFLINE_REFINER_INITIAL_SCAN_COUNT: "${APEX_OFFLINE_REFINER_INITIAL_SCAN_COUNT:-}"
      APEX_OFFLINE_REFINER_SUBMAP_WINDOW_SCANS: "${APEX_OFFLINE_REFINER_SUBMAP_WINDOW_SCANS:-}"
      APEX_OFFLINE_REFINER_POINT_STRIDE: "${APEX_OFFLINE_REFINER_POINT_STRIDE:-}"
      APEX_OFFLINE_REFINER_MAX_CORRESPONDENCE_M: "${APEX_OFFLINE_REFINER_MAX_CORRESPONDENCE_M:-}"
      APEX_OFFLINE_REFINER_UPDATE_PERIOD_S: "${APEX_OFFLINE_REFINER_UPDATE_PERIOD_S:-}"
      APEX_OFFLINE_REFINER_SEED_STATUS_TIMEOUT_S: "${APEX_OFFLINE_REFINER_SEED_STATUS_TIMEOUT_S:-}"
      APEX_OFFLINE_REFINER_SEED_MAX_MEDIAN_RESIDUAL_M: "${APEX_OFFLINE_REFINER_SEED_MAX_MEDIAN_RESIDUAL_M:-}"
      APEX_OFFLINE_REFINER_PUBLISH_GLOBAL_CORRECTION: "${APEX_OFFLINE_REFINER_PUBLISH_GLOBAL_CORRECTION:-}"
      APEX_OFFLINE_REFINER_ENABLE_INTER_WINDOW_ALIGNMENT: "${APEX_OFFLINE_REFINER_ENABLE_INTER_WINDOW_ALIGNMENT:-}"
      APEX_OFFLINE_REFINER_INTER_WINDOW_ALIGNMENT_GAIN: "${APEX_OFFLINE_REFINER_INTER_WINDOW_ALIGNMENT_GAIN:-}"
      APEX_OFFLINE_REFINER_INTER_WINDOW_MIN_OVERLAP_SCANS: "${APEX_OFFLINE_REFINER_INTER_WINDOW_MIN_OVERLAP_SCANS:-}"
      APEX_OFFLINE_REFINER_INTER_WINDOW_MIN_POINTS: "${APEX_OFFLINE_REFINER_INTER_WINDOW_MIN_POINTS:-}"
      APEX_OFFLINE_REFINER_INTER_WINDOW_MAX_POINTS: "${APEX_OFFLINE_REFINER_INTER_WINDOW_MAX_POINTS:-}"
      APEX_OFFLINE_REFINER_INTER_WINDOW_MAX_TRANSLATION_M: "${APEX_OFFLINE_REFINER_INTER_WINDOW_MAX_TRANSLATION_M:-}"
      APEX_OFFLINE_REFINER_INTER_WINDOW_MAX_YAW_DEG: "${APEX_OFFLINE_REFINER_INTER_WINDOW_MAX_YAW_DEG:-}"
      APEX_OFFLINE_REFINER_ENABLE_MANUAL_IDLE_FINALIZE: "${APEX_OFFLINE_REFINER_ENABLE_MANUAL_IDLE_FINALIZE:-}"
      APEX_OFFLINE_REFINER_MANUAL_STATUS_TOPIC: "${APEX_OFFLINE_REFINER_MANUAL_STATUS_TOPIC:-}"
      APEX_OFFLINE_REFINER_MANUAL_IDLE_TIMEOUT_S: "${APEX_OFFLINE_REFINER_MANUAL_IDLE_TIMEOUT_S:-}"
      APEX_OFFLINE_REFINER_MANUAL_MOTION_LINEAR_DEADBAND_MPS: "${APEX_OFFLINE_REFINER_MANUAL_MOTION_LINEAR_DEADBAND_MPS:-}"
      APEX_OFFLINE_REFINER_FINAL_MIN_SCAN_COUNT: "${APEX_OFFLINE_REFINER_FINAL_MIN_SCAN_COUNT:-}"
      APEX_OFFLINE_REFINER_SAVE_ON_FINALIZE: "${APEX_OFFLINE_REFINER_SAVE_ON_FINALIZE:-}"
      APEX_OFFLINE_REFINER_SAVE_OUTPUT_DIR: "${APEX_OFFLINE_REFINER_SAVE_OUTPUT_DIR:-}"
      APEX_STARTUP_COMPAT: "${APEX_STARTUP_COMPAT:-}"
      APEX_STAGGERED_STARTUP: "${APEX_STAGGERED_STARTUP:-}"
      APEX_SERIAL_WARMUP_S: "${APEX_SERIAL_WARMUP_S:-}"
      APEX_LIDAR_STARTUP_SETTLE_S: "${APEX_LIDAR_STARTUP_SETTLE_S:-}"
      APEX_NODE_STARTUP_STAGGER_S: "${APEX_NODE_STARTUP_STAGGER_S:-}"
      APEX_SERIAL_CONNECT_TOGGLE_DTR: "${APEX_SERIAL_CONNECT_TOGGLE_DTR:-}"
      APEX_SERIAL_CONNECT_DTR_LOW_S: "${APEX_SERIAL_CONNECT_DTR_LOW_S:-}"
      APEX_SERIAL_CONNECT_SETTLE_S: "${APEX_SERIAL_CONNECT_SETTLE_S:-}"
      APEX_SERIAL_FLUSH_INPUT_ON_CONNECT: "${APEX_SERIAL_FLUSH_INPUT_ON_CONNECT:-}"
      APEX_SERIAL_CONNECT_PROFILE_NAME: "${APEX_SERIAL_CONNECT_PROFILE_NAME:-}"
      APEX_SERIAL_NO_DATA_RECONNECT_S: "${APEX_SERIAL_NO_DATA_RECONNECT_S:-}"
      APEX_PARAMS_FILE: "${APEX_PARAMS_FILE:-}"
      APEX_BRIDGE_MIN_EFFECTIVE_SPEED_PCT: "${APEX_BRIDGE_MIN_EFFECTIVE_SPEED_PCT:-}"
      APEX_BRIDGE_MAX_SPEED_PCT: "${APEX_BRIDGE_MAX_SPEED_PCT:-}"
      APEX_BRIDGE_LAUNCH_BOOST_SPEED_PCT: "${APEX_BRIDGE_LAUNCH_BOOST_SPEED_PCT:-}"
      APEX_BRIDGE_LAUNCH_BOOST_HOLD_S: "${APEX_BRIDGE_LAUNCH_BOOST_HOLD_S:-}"
      APEX_BRIDGE_ACTIVE_BRAKE_ON_ZERO: "${APEX_BRIDGE_ACTIVE_BRAKE_ON_ZERO:-}"
      APEX_BRIDGE_ACTUATION_BACKEND: "${APEX_BRIDGE_ACTUATION_BACKEND:-}"
      APEX_ENABLE_LOCAL_ODOM_FUSION: "${APEX_ENABLE_LOCAL_ODOM_FUSION:-1}"
      APEX_ENABLE_SLAM_TOOLBOX: "${APEX_ENABLE_SLAM_TOOLBOX:-}"
      APEX_RECON_DIAGNOSTIC_MODE: "${APEX_RECON_DIAGNOSTIC_MODE:-}"
      APEX_RECON_FIXED_SPEED_PCT: "${APEX_RECON_FIXED_SPEED_PCT:-}"
      APEX_RECON_STEP_DURATION_S: "${APEX_RECON_STEP_DURATION_S:-}"
      APEX_RECON_LOG_PATH: "${APEX_RECON_LOG_PATH:-}"
      APEX_RECON_LOG_FLUSH_EVERY: "${APEX_RECON_LOG_FLUSH_EVERY:-}"
      APEX_RECON_LOG_OVERWRITE: "${APEX_RECON_LOG_OVERWRITE:-}"
      APEX_RECON_TIMEOUT_S: "${APEX_RECON_TIMEOUT_S:-}"
      APEX_RECON_MAX_RECOVERIES: "${APEX_RECON_MAX_RECOVERIES:-}"
      APEX_RECON_MIN_PROGRESS_M: "${APEX_RECON_MIN_PROGRESS_M:-}"
      APEX_RECON_LOG_LEVEL: "${APEX_RECON_LOG_LEVEL:-}"
      APEX_RESET_MAP_ON_START: "${APEX_RESET_MAP_ON_START:-}"
      APEX_SAVE_MAP_ON_COMPLETION: "${APEX_SAVE_MAP_ON_COMPLETION:-}"
      APEX_STEERING_CENTER_TRIM_DC: "${APEX_STEERING_CENTER_TRIM_DC:-}"
      APEX_STEERING_DIRECTION_SIGN: "${APEX_STEERING_DIRECTION_SIGN:-}"
      APEX_EXPLORE_MIN_SPEED_PCT: "${APEX_EXPLORE_MIN_SPEED_PCT:-}"
      APEX_EXPLORE_MAX_SPEED_PCT: "${APEX_EXPLORE_MAX_SPEED_PCT:-}"
      APEX_STEERING_GAIN: "${APEX_STEERING_GAIN:-}"
      APEX_FRONT_WINDOW_DEG: "${APEX_FRONT_WINDOW_DEG:-}"
      APEX_SIDE_WINDOW_DEG: "${APEX_SIDE_WINDOW_DEG:-}"
      APEX_FOV_HALF_ANGLE_DEG: "${APEX_FOV_HALF_ANGLE_DEG:-}"
      APEX_CENTER_ANGLE_PENALTY_PER_DEG: "${APEX_CENTER_ANGLE_PENALTY_PER_DEG:-}"
      APEX_WALL_AVOID_DISTANCE_M: "${APEX_WALL_AVOID_DISTANCE_M:-}"
      APEX_WALL_AVOID_GAIN_DEG_PER_M: "${APEX_WALL_AVOID_GAIN_DEG_PER_M:-}"
      APEX_GAP_ESCAPE_HEADING_THRESHOLD_DEG: "${APEX_GAP_ESCAPE_HEADING_THRESHOLD_DEG:-}"
      APEX_GAP_ESCAPE_RELEASE_DISTANCE_M: "${APEX_GAP_ESCAPE_RELEASE_DISTANCE_M:-}"
      APEX_GAP_ESCAPE_WEIGHT: "${APEX_GAP_ESCAPE_WEIGHT:-}"
      APEX_CORRIDOR_BALANCE_RATIO_THRESHOLD: "${APEX_CORRIDOR_BALANCE_RATIO_THRESHOLD:-}"
      APEX_CORRIDOR_FRONT_MIN_CLEARANCE_M: "${APEX_CORRIDOR_FRONT_MIN_CLEARANCE_M:-}"
      APEX_CORRIDOR_SIDE_MIN_CLEARANCE_M: "${APEX_CORRIDOR_SIDE_MIN_CLEARANCE_M:-}"
      APEX_CORRIDOR_FRONT_TURN_WEIGHT: "${APEX_CORRIDOR_FRONT_TURN_WEIGHT:-}"
      APEX_CORRIDOR_OVERRIDE_MARGIN_DEG: "${APEX_CORRIDOR_OVERRIDE_MARGIN_DEG:-}"
      APEX_CORRIDOR_MIN_HEADING_DEG: "${APEX_CORRIDOR_MIN_HEADING_DEG:-}"
      APEX_CORRIDOR_WALL_START_DEG: "${APEX_CORRIDOR_WALL_START_DEG:-}"
      APEX_CORRIDOR_WALL_END_DEG: "${APEX_CORRIDOR_WALL_END_DEG:-}"
      APEX_CORRIDOR_WALL_MIN_POINTS: "${APEX_CORRIDOR_WALL_MIN_POINTS:-}"
      APEX_WALL_FOLLOW_TARGET_DISTANCE_M: "${APEX_WALL_FOLLOW_TARGET_DISTANCE_M:-}"
      APEX_WALL_FOLLOW_GAIN_DEG_PER_M: "${APEX_WALL_FOLLOW_GAIN_DEG_PER_M:-}"
      APEX_WALL_FOLLOW_LIMIT_DEG: "${APEX_WALL_FOLLOW_LIMIT_DEG:-}"
      APEX_WALL_FOLLOW_ACTIVATION_HEADING_DEG: "${APEX_WALL_FOLLOW_ACTIVATION_HEADING_DEG:-}"
      APEX_WALL_FOLLOW_RELEASE_BALANCE_RATIO: "${APEX_WALL_FOLLOW_RELEASE_BALANCE_RATIO:-}"
      APEX_WALL_FOLLOW_MIN_CYCLES: "${APEX_WALL_FOLLOW_MIN_CYCLES:-}"
      APEX_WALL_FOLLOW_MAX_CLEARANCE_M: "${APEX_WALL_FOLLOW_MAX_CLEARANCE_M:-}"
      APEX_WALL_FOLLOW_FRONT_TURN_WEIGHT: "${APEX_WALL_FOLLOW_FRONT_TURN_WEIGHT:-}"
      APEX_STARTUP_CONSENSUS_MIN_HEADING_DEG: "${APEX_STARTUP_CONSENSUS_MIN_HEADING_DEG:-}"
      APEX_STARTUP_VALID_CYCLES_REQUIRED: "${APEX_STARTUP_VALID_CYCLES_REQUIRED:-}"
      APEX_STARTUP_GAP_LOCKOUT_CYCLES: "${APEX_STARTUP_GAP_LOCKOUT_CYCLES:-}"
      APEX_STARTUP_LATCH_CYCLES: "${APEX_STARTUP_LATCH_CYCLES:-}"
      APEX_AMBIGUITY_PROBE_SPEED_PCT: "${APEX_AMBIGUITY_PROBE_SPEED_PCT:-}"
      APEX_TURN_SPEED_REDUCTION: "${APEX_TURN_SPEED_REDUCTION:-}"
      APEX_MIN_TURN_SPEED_FACTOR: "${APEX_MIN_TURN_SPEED_FACTOR:-}"
      APEX_RECORD_DEBUG: "${APEX_RECORD_DEBUG:-0}"
      APEX_DEBUG_RUN_ID: "${APEX_DEBUG_RUN_ID:-}"
      APEX_DEBUG_OUTPUT_DIR: "${APEX_DEBUG_OUTPUT_DIR:-/work/ros2_ws/debug_runs}"
    volumes:
      - ../ros2_ws:/work/ros2_ws
      - ../../:/work/repo
      - /sys/class/pwm:/sys/class/pwm
    working_dir: /work/ros2_ws
    command: ["/bin/bash", "/work/ros2_ws/scripts/start_apex_pipeline.sh"]
